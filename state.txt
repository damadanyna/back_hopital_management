Etat panneau pour l'attribut pan_state : {1:"Disponible",2:"En réservation",3:"En location",4:"NonDispo/en attente de suppression"}

width:827px;height:1169px; // 100 dpi

//supression de duplication
DELETE
FROM pan_location
WHERE pan_loc_id NOT IN
(
SELECT MAX(pan_loc_id)
FROM pan_location
GROUP BY pan_id)